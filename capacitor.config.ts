import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.github.pa3ckp7.worktimetracker',
  appName: 'worktimetracker',
  webDir: 'dist'
};

export default config;
